# backend-rules (obrigatório)

- Não quebrar contrato (rotas/payload/status) sem migração explícita.
- Validar inputs; não confiar no cliente.
- Padronizar erros (status + mensagem) e não vazar dados sensíveis.
- Paginação/limites em listagens e buscas.

