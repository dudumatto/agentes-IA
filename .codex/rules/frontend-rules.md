# frontend-rules (obrigatório)

- Não quebrar consumo de API sem migração explícita.
- Sempre: `loading/error/empty/success`.
- Não hardcode de URL; usar env/config.
- A11y mínima: foco visível, labels, teclado.
- Evitar duplicação; extrair componentes/hooks.

