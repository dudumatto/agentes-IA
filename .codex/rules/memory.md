# Memória (Obsidian + repo)

- Salvar: decisões, padrões, bugs (não conversas).
- `MEMORY.md` deve ser um **índice** (links), não um dump.
- `LEARNINGS.md` deve ser “curado”, não histórico infinito.
- Loader deve ter perfis (mínimo vs completo) para token.

