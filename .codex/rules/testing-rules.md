# testing-rules (obrigatório)

- Não alterar lógica sem validação (teste ou checklist manual).
- Para bug: adicionar regressão mínima (quando houver infra).
- Cobrir: happy path + erro + borda + permissões (se aplicável).

