# spring-data-jpa

- Evitar N+1 com fetch planejado (`EntityGraph`, join fetch, projection).
- Paginacao para listagens.
- Queries por campos indexaveis quando houver volume.
- Nao fazer IO/query dentro de loop sem necessidade.

