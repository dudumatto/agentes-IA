# tailwind-layout

- Usar layout system consistente (`grid`, `flex`, `gap`).
- Evitar classes conflitantes no mesmo elemento.
- Preferir componentes/variants para padroes repetidos.
- Sem valores soltos quando houver token/escala existente.

