# implementation-workflow

1) Definir contrato (se houver API): endpoints/DTOs/status/migração.
2) Executar em passos pequenos (1 mudança por vez).
3) Se houver UI: `design-agent` antes de concluir.
4) Se houver auth/inputs/uploads: `security-agent` antes de concluir.
5) Validar localmente (quando aplicável).

